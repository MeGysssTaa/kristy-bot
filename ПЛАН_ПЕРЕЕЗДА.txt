не тильтуй, я старался((


1) Перенести все команды по образцу
   P.S.: не обязательно вручную каждый раз переписывать "def execute(...)" или копировать из других команд —
         можно просто начать писать "def ex" и пичарм сам тебе предложит полное описание метода (просто ENTER)

2) Перенести вложения, кнопки и прочий функционал из vk_cmds_disp.py (пока что в vklistener.py), причём:
   a) заменить vk_cmds.Rank → ranks.Rank
   b) заменить vk_cmds.senf → self.kristy.send

3) Удалить vk_cmds.py и vk_cmds_disp.py (они на этом этапе должны быть пустыми)

4) Заменить все использования groupsmgr → self.kristy.db

5) Удалить groupsmgr.py (на этом этапе у него не должно быть использований (можно чекнуть через ПКМ → Find Usages))

6) В dbmgr.py переименовать помеченные методы (не забыть про места, где они вызываются)

7) Если в коде каких-то команд сейчас используются запросы напрямую к БД (не через старый groupsmgr),
   то очень желательно (особенно, если запросы громоздкие) выделить их в отдельный метод в dbmgr

8) Посмотреть, нужно ли что-то делать с реализацией вложений и пр. в vklistener'е после переноса, или же всё норм

9) Удалить этот файл после переезда
